#!/usr/bin/env node

const command = process.argv[2];
const subcommand = process.argv[3];

const { configInstall, configUpdate, configSwitch } = require("../commands/config");
const { install } = require("../commands/install");
const { getRemoteDb, processDb, useDb } = require("../commands/db");
const { makeHtaccess } = require("../commands/htaccess");
const { setup } = require("../commands/setup");
const { activateComposerPlugins } = require("../commands/plugins");
const { envHelp, runWpEnv, e2eEnvHelp, runWpEnvE2e } = require("../commands/env");
const { compareHelp, compare } = require("../commands/compare");
const { e2eHelp, initE2e, generateE2eTests, runE2eTests } = require("../commands/e2e");
const { help } = require("../commands/help");

async function init() {
	switch (command) {
		case "config": {
			if (subcommand === "install") {
				await configInstall();
			} else if (subcommand === "update") {
				await configUpdate();
			} else if (subcommand === "switch") {
				await configSwitch();
			} else {
				console.log("Unknown config subcommand. Try:\n  wp-env-bin config install\n  wp-env-bin config update\n  wp-env-bin config switch");
			}
			break;
		}
		case "install": {
			await install();
			break;
		}
		case "db": {
			if (subcommand === "get") {
				await getRemoteDb();
			} else if (subcommand === "process") {
				await processDb();
			} else if (subcommand === "use") {
				await useDb(process.argv[4]);
			} else {
				console.log('Unknown subcommand. Try:\n  wp-env-bin db get\n  wp-env-bin db process\n  wp-env-bin db use <path/to/file.sql>');
			}
			break;
		}
		case "htaccess": {
			if (subcommand === "make") {
				await makeHtaccess();
			} else {
				console.log('Unknown subcommand. Try: wp-env-bin htaccess make');
			}
			break;
		}
		case "env": {
			if (subcommand === "setup") {
				setup(process.argv.slice(4));
			} else if (subcommand === "sync") {
				await getRemoteDb();
				await processDb();
				await makeHtaccess();
				await activateComposerPlugins();
			} else {
				const argv = process.argv.slice(3);
				if (!argv.length || argv[0] === "help" || argv[0] === "--help" || argv[0] === "-h") {
					envHelp();
				} else {
					runWpEnv(argv);
				}
			}
			break;
		}
		case "visual": {
			if (subcommand === "compare") {
				const argv = process.argv.slice(4);
				if (argv[0] === "help" || argv[0] === "--help" || argv[0] === "-h") {
					compareHelp();
				} else {
					await compare(argv);
				}
			} else {
				console.log('Unknown subcommand. Try: wp-env-bin visual compare');
			}
			break;
		}
		case "e2e": {
			if (subcommand === "help" || subcommand === "--help" || subcommand === "-h") {
				e2eHelp();
			} else if (subcommand === "env") {
				const envArgs = process.argv.slice(4);
				if (!envArgs.length || envArgs[0] === "help" || envArgs[0] === "--help" || envArgs[0] === "-h") {
					e2eEnvHelp();
				} else {
					runWpEnvE2e(envArgs);
				}
			} else if (subcommand === "init") {
				await initE2e();
			} else if (subcommand === "generate") {
				const type = process.argv[4]; // "editor" or "frontend"
				generateE2eTests(type, process.argv.slice(5));
			} else if (subcommand === "test") {
				runE2eTests(process.argv.slice(4));
			} else {
				console.log('Unknown e2e subcommand. Try:\n  wp-env-bin e2e init\n  wp-env-bin e2e test [--project=all-blocks-editor|all-blocks-frontend]\n  wp-env-bin e2e generate editor|frontend --file=path/to/block.json');
			}
			break;
		}
		case "help":
		default: {
			help();
			break;
		}
	}
}

init().catch((err) => {
	console.error(err.message || err);
	process.exit(1);
});
